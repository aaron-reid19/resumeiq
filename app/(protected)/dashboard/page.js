"use client"
import { useState } from "react"
import { useAuth } from "@/context/AuthContext"
export default function Page(){

    const { user } = useAuth();

    return (
        <main>
            
        </main>
        
    )
}